# Анализ корреляции между воздействием циклогептотриенов и изменениями физиологии/поведения простейших

## Как запустить

```bash
pip install -r ./requirements.txt
python src/main.py --input data/example_data.csv --output reports/summary.pdf
```
